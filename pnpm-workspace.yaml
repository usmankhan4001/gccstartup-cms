# pnpm 11.5 reads the build-script allowlist here as an `allowBuilds` map.
# sharp = image processing, esbuild = TS bundling — both need their install scripts.
allowBuilds:
  esbuild: true
  sharp: true
